# Backup Watchdog

Автоматический скрипт для архивации файлов и папок.

## Описание

Скрипт `backup-watchdog.sh` мониторит указанную папку и автоматически упаковывает в `.tar.gz` любые новые файлы и подпапки, после чего удаляет оригиналы.

## Использование